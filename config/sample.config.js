export default {
    flagpole: {
        registerEnabled: true
    },
    auth: {
        secret: 'auth-secret'
    },
    aws: {
        bucket: 'bucket-name',
        path: 'path/to/folder/',
        accessKey: 'aws-access-key',
        secretKey: 'aws-secret-key'
    }
};
