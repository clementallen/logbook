var config = {
    auth: {
        secret: 'auth-secret-here'
    },
    aws: {
        accessKey: '',
        secret: ''
    },
    tempMessage: 'temporary homepage message'
};

module.exports = config;
